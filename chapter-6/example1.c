#include <stdio.h>

int main() {
  char month[10] = "January";
  char myName[] = "Chris";

  printf("%c\n", month[0]);
  printf("The month is %s", month);

  printf("\nMy name is %s", myName);

  return 0;
}